<%- include('partials/header') -%>
<!-- <ul>
    <% data.Search.forEach(function(movie){ %>
    <li>
        <a href="/results/<%= movie.imdbID %>"><strong><%= movie.Title %></strong></a> - <%= movie.Year%> ,
        <%= movie.Type %>
    </li>
    <% }); %>
</ul> -->

<div class="container">
    <!-- <div class="row justify-content-around">
        <ul class="list-unstyled">
            <% data.Search.forEach(function(movie){ %>
            <li class="media">
                <img class="img-thumbnail" src="<%= movie.Poster%>" alt="<%= movie.Title %>">
                <div class="media-body">
                    <h1><%= movie.Title %></h1>
                    <h2><%= movie.Year %></h2>
                    <a href="/results/<%= movie.imdbID %>" class="btn btn-primary btn-sm">View More</a>
                </div>
            </li>
            <% }); %>
        </ul>
    </div> -->

    <div class="row justify-content-start mb-5" style="display: flex; flex-wrap: wrap;">
        <% data.Search.forEach(function(movie){ %>
        <div class="col-sm-6 col-md-4 col-lg-3 mb-4 text-center">
            <a href="/results/<%= movie.imdbID %>">
                <img class="" height="300px" src="<%= movie.Poster %>" alt="<%= movie.Title %>">
                <h4 class="mt-3"><%= movie.Title %></h4>
                <!-- <span class="text-right"><%= movie.Year %></span> -->
            </a>
        </div>
        <!-- <a href="/results/<%= movie.imdbID %>" class="btn btn-primary btn-sm">View More</a> -->
        <% }); %>
    </div>


    <form class="text-center" action="/" method="GET">
        <input class="btn btn-danger btn-lg" type="submit" value="Search More!">
    </form>
</div>

<%- include('partials/footer') -%>